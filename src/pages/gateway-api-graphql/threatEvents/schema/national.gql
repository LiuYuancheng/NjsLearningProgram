#
# Queries
#
type Query  {
   threatEvents_nationalCount(
        queryType:String!
        fieldStr:String
        threatType:String
        limitVal:Int
    ):JSON

   threatEvents_nationalTopN(
        dimension:String!
        filterDimension:String
        filterVal:String
        topN:Int
    ):JSON
}